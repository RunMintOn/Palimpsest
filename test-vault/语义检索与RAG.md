# Embedding、RAG 与语义检索

Embedding 模型将中文文本编码为向量。检索时使用 query vector 与文档片段的 cosine similarity 排序。Embedding 模型将中文文本编码为向量。检索时使用 query vector 与文档片段的 cosine similarity 排序。Embedding 模型将中文文本编码为向量。检索时使用 query vector 与文档片段的 cosine similarity 排序。Embedding 模型将中文文本编码为向量。检索时使用 query vector 与文档片段的 cosine similarity 排序。Embedding 模型将中文文本编码为向量。检索时使用 query vector 与文档片段的 cosine similarity 排序。Embedding 模型将中文文本编码为向量。检索时使用 query vector 与文档片段的 cosine similarity 排序。Embedding 模型将中文文本编码为向量。检索时使用 query vector 与文档片段的 cosine similarity 排序。Embedding 模型将中文文本编码为向量。检索时使用 query vector 与文档片段的 cosine similarity 排序。

## RAG

RAG 先从本地 Markdown 知识库找回最相关的片段，再把它们提供给生成模型或写作界面。


