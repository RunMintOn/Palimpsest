# Palimpsest Agent 指南

## 先做什么

1. 先读取对应 issue/spec、相关源码和现有测试，再决定改动范围。
2. 先区分根问题、观察到的现象和假设；不要把 `mtime`、文件大小或 Vault 事件直接当成正文语义变化。
3. 保留用户已有的工作区改动。提交或暂存只有在用户明确要求时执行。
4. 代码真相源是 `src/`；`main.js` 是构建产物，不直接编辑。

详细的构建、测试、同步、索引恢复和测试 Vault 流程见 [`MAINTENANCE.md`](MAINTENANCE.md)。

## 验证与交付

完成代码改动后，根据影响范围运行：

```bash
npm run typecheck
npm test
npm run build
```

涉及 Windows 插件目录时，只有在用户要求同步后才运行：

```bash
bash scripts/sync-to-windows.sh
```

该脚本只同步构建运行文件，并保留目标目录的 `data.json`。不要用复制构建产物的方式覆盖用户索引或设置。

## 索引领域约束

### 索引范围

- 设置中的排除目录是**期望范围**；`PersistentIndex.scope` 是**实际生效范围**。
- 两者不一致时，界面必须明确显示 pending；保存设置不等于已经应用索引范围。
- “应用索引范围变化”是独立的增量操作：只扫描新纳入的文件、删除新排除的记录；它不等同于全量重建。
- 全量重建仍是独立操作，成功提交前保留旧索引。
- 用户取消范围应用或大规模更新后，pending 状态和可执行的手动操作必须保留；不要把取消伪装成已应用。
- 普通大规模增量更新可以保持 deferred，但用户明确发起的范围应用可以单独执行，不应因此隐藏范围应用按钮。

### 文档变化与向量复用

- `path`、`mtime`、文件大小和 Vault 事件只能用于发现候选变化；它们不是正文语义变化的证据。
- 标准合法 YAML frontmatter 不进入 Markdown 切块的正文语义输入。
- 向量输入的语义比较基于文件名、标题层级（breadcrumb）和片段文本；行号、chunk ID 和源文件统计信息不应单独触发“语义修改”。
- 文档级摘要应区分：新增、真正语义变化、仅状态变化、无法索引和删除。
- 向量可复用表示无需重新 embedding，不代表本次完全不需要更新文档元数据或切块位置。

### 索引安全

- 索引存放在按 Vault 隔离的 IndexedDB 中；不要把插件目录迁移或构建同步误当成索引迁移。
- 任何可能删除或替换索引的操作都必须经过现有的计划、确认、执行和 durable commit 流程。
- 计划在扫描和执行之间可能过期；变更 Vault、配置、身份或范围时必须保留 stale-plan 校验。
- 对索引边界情况优先在纯函数/计划模块的公开 seam 增加回归测试；不要为了覆盖率测试内部私有实现。

## 改动完成标准

- 现象对应的回归测试能在修复前失败、修复后通过。
- `typecheck`、相关测试和必要的构建均通过。
- 临时诊断日志、一次性脚本和调试产物已删除，或明确放入被忽略的调查目录。
- 最终说明改了什么、验证了什么、哪些行为仍未在真实 Obsidian 环境验证。
